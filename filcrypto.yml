---
GENERATOR:
  PackageName: generated
  PackageDescription:
  PackageLicense:
  Options:
    SafeStrings: true
  Includes:
    - ../filcrypto.h
  FlagGroups:
    - {name: LDFLAGS, flags: ["-L${SRCDIR}/.. -lfilcrypto_v1 -lfilcrypto_v2"]}
    - {name: pkg-config, flags: ["${SRCDIR}/../filcrypto-v1.pc" "${SRCDIR}/../filcrypto-v2.pc"]}

PARSER:
  Defines:
  IncludePaths:
    - ./headerstubs/
  SourcesPaths:
    - ./filcrypto.h

TRANSLATOR:
  ConstRules:
    defines: expand
    enum: cgo
  PtrTips:
    function:
      - {target: "^fil_destroy", tips: [ref]}
  Rules:
    global:
      - {action: accept, from: "^fil"}
      - {action: accept, from: "^FCPResponseStatus"}
      - {transform: export}
    private:
      - {transform: unexport}
    post-global:
      - {transform: export}
      - {load: snakecase}
